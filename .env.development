# Development Environment Defaults (non-secrets only)
# Secrets should be in .env.local (not committed)

# Node environment
NODE_ENV=development

# Database defaults for local development
# The actual connection is handled by prisma.db.ts with fallback to file:./dev.db
# LOCAL_DATABASE_URL is only needed for Prisma CLI commands

# Logging level
LOG_LEVEL=debug
