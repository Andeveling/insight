# ============================================================
# INSIGHT - Environment Variables Reference
# ============================================================
# Copy this file to `.env.local` for local development
# See docs/ENVIRONMENTS.md for complete setup guide
# ============================================================

# -----------------------------------------------------------
# DATABASE - Turso (libSQL)
# -----------------------------------------------------------
# LOCAL DEVELOPMENT: Uses SQLite file via libSQL adapter
# No need to set DATABASE_URL locally - defaults to file:./prisma/dev.db

# LOCAL_DATABASE_URL is ONLY used by Prisma CLI for migrations
# This is NOT needed in Vercel (production/preview)
LOCAL_DATABASE_URL="file:./prisma/dev.db"

# PRODUCTION/PREVIEW: Set these in Vercel Dashboard
# TURSO_DATABASE_URL="libsql://[your-db].turso.io"
# TURSO_AUTH_TOKEN="your-turso-auth-token"

# -----------------------------------------------------------
# AUTHENTICATION - Better Auth
# -----------------------------------------------------------
# Generate secret: openssl rand -base64 32
BETTER_AUTH_SECRET="your-secret-key-here-min-32-chars"

# Auth callback URL (must match your deployment URL)
# Local: http://localhost:3000
# Preview: https://your-branch.vercel.app
# Production: https://your-domain.com
BETTER_AUTH_URL="http://localhost:3000"

# -----------------------------------------------------------
# AI PROVIDERS - For Reports Feature
# -----------------------------------------------------------
# OpenAI - https://platform.openai.com/api-keys
OPENAI_API_KEY="sk-..."

# Google AI (optional) - https://makersuite.google.com/app/apikey
# GOOGLE_GENERATIVE_AI_API_KEY="..."

# -----------------------------------------------------------
# EMAIL - Resend
# -----------------------------------------------------------
# Resend API Key - https://resend.com/api-keys
RESEND_API_KEY="re_..."

# Email sender address (optional, defaults to onboarding@resend.dev)
# For production, verify a domain at https://resend.com/domains
# EMAIL_FROM="Insight <notifications@your-domain.com>"

# -----------------------------------------------------------
# FEEDBACK SYSTEM
# -----------------------------------------------------------
# Salt for anonymizing feedback responses (required for anonymous feedback)
# Generate: openssl rand -hex 32
FEEDBACK_ANONYMIZATION_SALT="your-random-salt-here"

# Cron job secret (required for Vercel Cron protection)
# Generate: openssl rand -hex 32
CRON_SECRET="your-cron-secret-here"

# -----------------------------------------------------------
# VERCEL ENVIRONMENT (auto-set by Vercel)
# -----------------------------------------------------------
# VERCEL_ENV="development" | "preview" | "production"
# VERCEL_URL="your-deployment.vercel.app"
# VERCEL_GIT_COMMIT_SHA="..."
# VERCEL_GIT_COMMIT_REF="branch-name"
